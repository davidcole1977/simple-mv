(function () {
  'use strict';

  var expect = require('chai').expect,
      sinon = require('sinon'),
      utHelpers = require('../lib/unit-test-helpers.js'),
      module = require(utHelpers.getModulePath('simple-datum'));

  xdescribe('simple-datum', function () {
    
    describe('create()', function () {
      it('stuff', function () {
        
      });
    });

  });

})();
